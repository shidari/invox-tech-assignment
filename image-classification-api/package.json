{
	"name": "image-classification-api",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"generate-migrations": "pnpm exec drizzle-kit generate",
		"migrate": "pnpm exec drizzle-kit migrate",
		"push-db": "pnpm exec  drizzle-kit push"
	},
	"dependencies": {
		"drizzle-orm": "^0.44.5",
		"hono": "^4.9.6"
	},
	"devDependencies": {
		"@types/node": "^24.4.0",
		"dotenv": "^17.2.2",
		"drizzle-kit": "^0.31.4",
		"wrangler": "^4.4.0"
	}
}
